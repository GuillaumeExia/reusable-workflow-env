name: CallerWorkflow

on:
  workflow_dispatch:

jobs:
  reusable_job:
    name: Get File Content
    uses: ./.github/workflows/reusable.yml
  
  read_env:
    name: Read Output
    runs-on: ubuntu-latest
    needs: reusable_job
    steps:
      - name: Read from Output
        run: |
          echo -e "$my_file"